import { Canvas, Controls, Meta } from '@storybook/blocks';

import * as ImageModalStories from './ImageModal.stories';

<Meta of={ImageModalStories} />

# Image Modal

A modal to add an image to the markdown editor.

This only works with a link to a remote image and doesn't allow to upload one.

See Stories for a preview.

## Props

<Controls />
