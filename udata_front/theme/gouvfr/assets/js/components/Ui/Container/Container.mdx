import { Canvas, Controls, Meta } from '@storybook/blocks';

import * as Container from './Container.stories';

<Meta of={Container} />

# Container

This component is a simple container with a white background and some padding.
Useful when the page background is a light grey.


<Canvas of={Container.SimpleContainer} />

## Props

<Controls />
