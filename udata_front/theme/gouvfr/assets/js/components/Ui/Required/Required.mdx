import { Canvas, Meta } from '@storybook/blocks';

import * as RequiredStories from './Required.stories';

<Meta of={RequiredStories} />

# Required

A simple component that show a branded `*`.
In most cases, you should use the ready-made group components instead :
 - [Input Group](?path=/docs/components-inputgroup--docs)
 - [Select Group](?path=/docs/components-selectgroup--docs)

 <Canvas of={RequiredStories.RequiredLabel} />
